const nextConfig = {
  experimental: {
    serverActions: true,
  },
  // ❌ no pongas esto:
  // i18n: {
  //   locales: ['en', 'es'],
  //   defaultLocale: 'en',
  // }
};

export default nextConfig;
